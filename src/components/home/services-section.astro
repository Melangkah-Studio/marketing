---
import { Image } from "astro:assets";
import Service1 from "@/assets/imgs/services/service-1.png";
import Service2 from "@/assets/imgs/services/service-2.png";
import Service3 from "@/assets/imgs/services/service-3.png";
import Service4 from "@/assets/imgs/services/service-4.png";

const services = [
  {
    alt: "Website & App Design",
    description:
      "We design websites and mobile experiences that balance clarity, personality, and usability — turning ideas into digital products that feel as good as they work.",
    image: Service1,
    tags: [
      "Dashboard",
      "Mobile App",
      "E-Commerce",
      "Prototyping",
      "Landing Page",
      "Company Profile",
    ],
    title: "Website & App Design",
  },
  {
    alt: "Software Development",
    description:
      "We bring designs to life through functional, responsive, and purposeful digital experiences. Whether it's a marketing website or a digital product, we build with the experience in mind from the very first step",
    image: Service2,
    tags: [
      "Integrations",
      "Experience Website",
      "CMS",
      "Website Responsive",
      "Mobile App Development",
    ],
    title: "Software Development",
  },
  {
    alt: "AI & ML Development",
    description:
      "We explore how AI and emerging technologies can become part of the way brands, products, and people work — from intelligent features to new digital experiences.",
    image: Service3,
    tags: [
      "RAG",
      "AI Agents",
      "AI Chatbots",
      "OCR Solutions",
      "Computer Vision",
      "Machine Learning",
    ],
    title: "AI & ML Development",
  },
  {
    alt: "AI Transformation",
    description:
      "Our flagship end-to-end AI transformation service, helping organizations turn emerging ideas into meaningful, working solutions. From research and discovery to development, implementation, and continuous performance monitoring, we stay with the work from its first step to what comes next",
    image: Service4,
    tags: [
      "AI Strategy",
      "AI Consulting",
      "AI Automation",
      "AI Integration",
      "Workflow Automation",
    ],
    title: "Melangkah",
  },
];
---

<section id="section-4">
  <h2>Things We Make & Explore</h2>
  <div class="content-container">
    {services.map((service, index) => (
        <article
          class="content"
          style={`--stack-index: ${index};`}
        >
          <header class="content-header">
            <span class="number">{String(index + 1).padStart(2, "0")}</span>
            <h3>{service.title}</h3>
            <div class="tags">
              {service.tags.map((tag) => (
                <span>{tag}</span>
              ))}
            </div>
          </header>
          <div class="content-body">
            <p>{service.description}</p>
            <div class="image-container">
              <Image alt={service.alt} src={service.image} />
            </div>
          </div>
        </article>
      ))}
  </div>
</section>

<style>
  #section-4 {
    position: relative;
    padding-bottom: 1rem;
    background-color: white;
  }

  h2 {
    padding: 1rem 1rem 2rem;
    font-size: 2rem;
    font-weight: 700;
    color: #333333;
    letter-spacing: -0.05em;
  }

  .content-container {
    display: flex;
    flex-direction: column;
    padding: 1rem;
  }

  .content-container::after {
    flex: none;
    height: 50svh;
    content: "";
  }

  .content {
    --stack-step: 8.5rem;

    position: sticky;
    top: calc(1rem + var(--stack-index) * var(--stack-step));
    display: flex;
    flex-direction: column;
    height: calc(100svh - 2rem - var(--stack-index) * var(--stack-step));
    overflow: clip;
    background-color: white;
    border-top: 1px solid #dcdcdc;
  }

  .content-header {
    display: grid;
    flex: none;
    grid-template-areas:
      "number title"
      "tags tags";
    grid-template-columns: 0.5fr 3fr;
    height: var(--stack-step);
    padding: 1rem;
  }

  .content-body {
    display: grid;
    flex: 1;
    grid-template-rows: min-content minmax(0, 1fr);
    gap: 1rem;
    min-height: 0;
    padding: 0 1rem 1rem;
  }

  .content:last-child {
    border-bottom: 1px solid #dcdcdc;
  }

  .number {
    grid-area: number;
    align-self: center;
    font-size: 1.5rem;
    font-weight: 500;
    color: #333333;
    letter-spacing: -0.05em;
  }

  .content h3 {
    grid-area: title;
    align-self: center;
    font-size: 1.5rem;
    font-weight: 500;
    color: #333333;
    letter-spacing: -0.05em;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    grid-area: tags;
    gap: 0.5rem;
    align-items: center;
    justify-content: start;
    padding-top: 1rem;
  }

  .tags span {
    padding: 0.25rem 0.5rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: #333333;
    letter-spacing: -0.05em;
    text-wrap: nowrap;
    text-decoration: none;
    background-color: #edebde;
  }

  .content p {
    align-self: start;
    font-size: 1rem;
    color: #333333;
    letter-spacing: -0.05em;
    text-wrap: balance;
  }

  .image-container {
    width: 100%;
    min-height: 0;
    overflow: hidden;
  }

  .content img {
    width: 100%;
    height: 100%;
    pointer-events: none;
    object-fit: cover;
    object-position: center;
  }

  @media (min-width: 768px) {
    #section-4 {
      display: grid;
      grid-template-columns: minmax(15rem, 0.7fr) minmax(0, 1.3fr);
      gap: 1rem;
      align-items: start;
      padding: 1rem;
    }

    h2 {
      position: sticky;
      top: 1rem;
      padding: 0;
      font-size: 3rem;
    }

    .content-container {
      padding: 0;
    }

    .content {
      --stack-step: 9.5rem;

      top: calc(1rem + var(--stack-index) * var(--stack-step));
      height: calc(100svh - 2rem - var(--stack-index) * var(--stack-step));
    }

    .content-header {
      grid-template-areas: "number title tags";
      grid-template-columns: 0.25fr 3fr 1.5fr;
      height: var(--stack-step);
      padding: 2rem;
    }

    .content-body {
      grid-template-rows: minmax(0, 1fr);
      grid-template-columns: minmax(0, 2fr) minmax(14rem, 1fr);
      gap: 2rem;
      align-items: end;
      padding: 0 2rem 2rem;
    }

    .number {
      align-self: self-start;
      font-size: 2rem;
    }

    .content h3 {
      align-self: self-start;
      font-size: 2rem;
    }

    .tags {
      padding-top: 0;
    }

    .tags span {
      font-size: 1rem;
    }

    .content p {
      font-size: 1.25rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .content-container::after {
      display: none;
    }

    .content {
      position: relative;
      top: auto;
      height: auto;
      overflow: visible;
    }

    .content-header {
      height: auto;
    }

    .image-container {
      aspect-ratio: 1.188 / 1;
    }
  }
</style>
