---
import { Image } from "astro:assets";
import Hero from "@/assets/imgs/hero.png";
---

<section id="section-1">
  <h2 class="melangkah">Melangkah</h2>
  <div class="hero-container">
    <Image alt="Melangkah Studio Hero Image" class="hero" priority src={Hero} />
    <h2 class="studio">Studio</h2>
  </div>
</section>

<style>
  #section-1 {
    position: relative;
    display: flex;
    flex-direction: column;
    height: 100vh;
    overflow: hidden;
  }

  .melangkah {
    position: relative;
    bottom: -1px;
    left: 4px;
    z-index: 1;
    font-size: clamp(3rem, 10vw, 7rem);
    font-weight: 800;
    color: #333333;
    letter-spacing: -0.05em;
    text-box: trim-end cap alphabetic;
  }

  .hero-container {
    position: relative;
    flex: 1 1 0;
    min-height: 0;
  }

  .hero {
    width: 100%;
    height: 100%;
    aspect-ratio: 1.76 / 1;
    pointer-events: none;
    object-fit: cover;
    object-position: 90% 0%;
  }

  .studio {
    position: absolute;
    right: 0;
    bottom: 1px;
    font-size: clamp(5rem, 10vw, 7rem);
    font-weight: 800;
    color: white;
    letter-spacing: -0.05em;
    text-box: trim-both ex alphabetic;
  }
</style>
